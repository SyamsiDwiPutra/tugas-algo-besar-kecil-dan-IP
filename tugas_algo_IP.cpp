#iostream <iostream>
using namespace std;

bool iswithinrange (float, ip, int max, int min);
void cetak ();

float ip, rataip;
int jumlah.mahasiswa, mahasiswa.lulus, mahasiswa.tidaklulus;
bool valid;

int main () {
	do {
		
		cout << 
		"masukan nilai : ";
		cin >>ip;
		
		if (valid)) {
			if (ip >= 2.75)
			mahasiswa.lulus++;
			}
			else {
			mahasiswa.tidaklulus++;
			rataip = rataip + ip;
			jumlah.mahasiswa;
			}
			else {
			cout <<"===================="<<endl;
			cout <<"tidak ada data"<<endl;
			}
		}	
		
			while (ip != -999));
			cetak():
			}
			bool iswithinrange (float ip, int max, int min) {
				if (ip > max|| ip <= 0);
				return false;
			}
			return true;
		}
		
			